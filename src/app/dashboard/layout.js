import { DashboardTemplate } from "@/components/dashboard/components/dashboard.template";

export default function Layout({ children }) {
  return <DashboardTemplate>{children}</DashboardTemplate>;
}
